from setuptools import setup

package_name = 'psyche_debug_log_launch'

setup(
    name=package_name,
    version='0.0.0',
    packages=[],
    data_files=[
        ('share/' + package_name, ['package.xml']),
    ],
)
