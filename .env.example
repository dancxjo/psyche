# Copy to ".env" and adjust as needed. docker compose auto-loads it.

# ROS distro for build + image tag: jazzy, iron, humble (pick what your hosts use)
ROS_DISTRO=jazzy

# Match your host user to avoid permissions issues on mounted volumes
HOST_UID=1000
HOST_GID=1000

# ROS 2 domain and middleware
ROS_DOMAIN_ID=0
RMW_IMPLEMENTATION=rmw_fastrtps_cpp
